import 'package:flutter/material.dart';

class ScheduleError extends StatelessWidget {
  const ScheduleError({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('error'),
    );
  }
}
